architectury {
    common(rootProject.enabled_platforms.split(','))
}

dependencies {
    implementation "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"

    implementation "dev.architectury:architectury:${rootProject.architectury_api_version}"

    compileOnly "org.yaml:snakeyaml:${rootProject.snake_version}"
    compileOnly "com.github.spacemex:SimpleConfigApi:${rootProject.simple_config_version}"
}